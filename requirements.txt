# Python dependencies mounted into the Semaphore container
# at /etc/semaphore/requirements.txt and installed at startup.

# WinRM transports
pywinrm>=0.4.3
pypsrp>=0.9.0

# Kerberos support (optional, comment out if not needed)
# requests-kerberos>=0.14.0

# YAML linting (used by validate.sh)
yamllint>=1.35.0

# Ansible lint
ansible-lint>=24.0.0

# Jinja2 extras
jinja2>=3.1.0
